// cubeVolume.mjs

// Function to calculate volume of a cube
export function volumeOfCube(side) {
    return Math.pow(side, 3); // side³
  }
  

  /*
  function volumeOfCube(side) {
  return Math.pow(side, 3);
}

function surfaceAreaOfCube(side) {
  return 6 * side * side;
}

export { volumeOfCube, surfaceAreaOfCube };
*/

/*
const volumeOfCube = (side) => side ** 3;
export default volumeOfCube;
*/